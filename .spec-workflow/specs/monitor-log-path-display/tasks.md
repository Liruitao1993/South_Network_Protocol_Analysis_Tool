# 任务文档：监控器日志路径显示与浏览

## 任务列表

### 任务1：添加路径显示和浏览按钮控件

**文件**: `monitor_widget.py`
**修改位置**: `_build_ui` 方法，`filter_toolbar` 区域（约 L258-267）

**修改内容**:
1. 在 `csv_status_label` 后添加 `csv_path_label`（路径显示标签）
2. 在 `csv_path_label` 后添加 `browse_log_btn`（浏览按钮）
3. 设置样式：等宽字体、灰色默认色、可选中文本

**验收标准**:
- [x] 路径标签显示"路径: 未记录"（灰色）
- [x] 浏览按钮固定宽度45px，显示"浏览"
- [x] 浏览按钮连接到 `_open_log_directory` 方法

---

### 任务2：修改记录开始逻辑，显示路径

**文件**: `monitor_widget.py`
**修改位置**: `_start_csv_recording` 方法（约 L1057-1074）

**修改内容**:
1. 在设置 `csv_status_label` 后，更新 `csv_path_label` 显示完整路径
2. 路径样式改为深色（记录中状态）

**验收标准**:
- [x] 点击"开始记录"后，路径标签显示完整路径（如 `路径: Output/monitor_log_20260729_143022.csv`）
- [x] 路径使用等宽字体显示

---

### 任务3：修改记录停止逻辑，保留路径显示

**文件**: `monitor_widget.py`
**修改位置**: `_stop_csv_recording` 方法（约 L1076-1087）

**修改内容**:
1. 停止记录后，保留 `csv_path_label` 显示最后记录的路径
2. 路径样式改为灰色（已停止状态）

**验收标准**:
- [x] 停止记录后，路径标签保留显示（灰色）
- [x] 再次开始记录时，路径更新为新文件

---

### 任务4：实现打开日志目录功能

**文件**: `monitor_widget.py`
**修改位置**: 新增 `_open_log_directory` 方法

**修改内容**:
1. 获取 `Output` 目录的绝对路径
2. 确保目录存在（如不存在则创建）
3. 使用 `os.startfile` (Windows) 或 `xdg-open` (Linux/Mac) 打开目录
4. 打开失败时静默忽略

**验收标准**:
- [x] 点击"浏览"按钮，资源管理器打开 `Output` 目录
- [x] 目录不存在时自动创建
- [x] 打开失败时不弹窗

---

### 任务5：清理和测试

**文件**: `monitor_widget.py`

**修改内容**:
1. 检查所有修改是否符合代码风格
2. 确保不影响现有CSV记录和导出功能
3. 验证UI布局美观

**验收标准**:
- [x] 语法检查通过
- [x] 不影响现有CSV记录和导出功能
- [x] UI布局符合现有风格
